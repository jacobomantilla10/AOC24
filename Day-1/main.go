package main

import "fmt"

func main() {
	// Read from file into two separate arrays
	// sort separate arrays
	// loop through both arrays taking the difference from each other
	fmt.Println(prob2("test1.txt"))
}
